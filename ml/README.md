# Machine-learning artifacts

M00 contains manifests and governance only. Raw datasets, model weights, training outputs and private voice data are not tracked.

LoRA/QLoRA work cannot start before logging, consent, redaction, evaluation, lineage and rollback exist.
