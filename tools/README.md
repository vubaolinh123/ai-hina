# Tools

- `dev`: workspace validation, hardware inventory and gate orchestration.
- `license`: provenance checks.
- `sbom`: CycloneDX generation.

Bootstrap tools use the Python/Node standard library so M00 does not require unreviewed runtime dependencies.
