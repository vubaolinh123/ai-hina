# Adapters

Domain adapters planned after the companion gates:

- `minecraft`: Mineflayer deterministic controller and state verifier.
- `stream`: platform normalization, rate limiting and policy enforcement.
- `livekit`: optional multi-user voice adapter.

Vendor types must not cross the adapter boundary.
