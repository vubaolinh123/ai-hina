[project]
name = "hina-safety-policy"
version = "0.1.0"
description = "Local-first capability policy and audit authority for Hina AI"
requires-python = ">=3.12,<3.15"
license = "MIT"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hina_safety"]
