[project]
name = "hina-avatar"
version = "0.1.0"
description = "Typed avatar state and cue service for Hina AI"
requires-python = ">=3.12,<3.15"
license = "MIT"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hina_avatar"]
