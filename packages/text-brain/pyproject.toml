[project]
name = "hina-text-brain"
version = "0.1.0"
description = "Local-first text model gateway and conversation orchestration for Hina AI"
requires-python = ">=3.12,<3.15"
license = "MIT"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hina_text_brain"]
