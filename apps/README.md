# Applications

Planned deployable applications:

- `core-runtime`: Python orchestration runtime, starting in M01/M03.
- `desktop`: Electron/Vue stage and operator UI, starting in M07.
- `dev-console`: local contract/replay console.

Each application must have its own manifest and tests before code is added.
